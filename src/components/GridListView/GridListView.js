import React, { Fragment } from 'react';

import Button from '../GridListView/GridListView';

const gridListView = props => {
    return (
        <Fragment>
            <Logo />
        </Fragment>
    )
}

export default gridListView;